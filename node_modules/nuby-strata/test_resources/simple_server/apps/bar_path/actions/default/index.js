var nuby_strata = require('./../../../../../../lib');
var strata = require('../../../../../../node_modules/strata/lib');
var smr = nuby_strata.render;
var util = require('util');

module.exports = {
    data:function (params, callback) {

        callback(null, {bar:10});
    },

    get_route:function () {
        return '/' + this.controller.name;
    },

    name:'bar_path_index'

}